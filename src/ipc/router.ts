import { app } from "./app";
import { shell } from "./shell";
import { theme } from "./theme";
import { unload } from "./unload";
import { window } from "./window";

export const router = {
  app,
  shell,
  theme,
  unload,
  window,
};
